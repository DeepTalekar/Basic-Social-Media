name: Shutdown

on:
  schedule:
    - cron: '02 17 * * *'

jobs:
  shutdown:
    environment:
      name: 'Production'
    runs-on: ubuntu-latest
    steps:
      - name: Get NodeJS
        uses: actions/setup-node@v3
        with:
          node-version: 16    # Railway requires NodeJS version 16 or higher
      - name: Install Railway
        run: npm i -g @railway/cli
      - name: Run Railway Link
        env:
          RAILWAY_TOKEN: ${{secrets.RAILWAY_TOKEN}}
          RAILWAY_PROJECT_ID: ${{secrets.RAILWAY_PROJECT_ID}}
        run: railway link ${{secrets.RAILWAY_PROJECT_ID}}
      - name: Link to the service
        run: railway service ${{secrets.RAILWAY_SERVICE}}
        env:
          RAILWAY_TOKEN: ${{secrets.RAILWAY_TOKEN}}
      - name: Shutting down production
        if: github.event.schedule >= '02 17 * * *'
        run: railway down --yes
        env:
          RAILWAY_TOKEN: ${{secrets.RAILWAY_TOKEN}}
